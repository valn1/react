import React from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
} from 'react-native';

const App: () => React$Node = () => {
    return (
        <>
            <SafeAreaView>
                <Text>asdaqsd</Text>
            </SafeAreaView>
        </>
    );
};

const styles = StyleSheet.create({

});

export default App;
